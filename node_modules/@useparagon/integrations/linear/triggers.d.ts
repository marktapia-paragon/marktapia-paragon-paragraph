import { Operator } from '@useparagon/core/resolvers';
import { ConditionalInput } from '@useparagon/core/steps/library/conditional';

/**
 * New Issue Created intent type
 */
type LinearTriggerIssueCreated = {
  /**
   * Team ID
   * Use Connect Portal User Settings to allow users to select a Linear Team's ID.  Leaving this blank will trigger this workflow for every public team.
   *
   * **Example**:
   * ```
   * a70bdf0f-530a-4887-857d-46151b52b47c
   * ```
   */
  teamId?: string;

  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Status Updated intent type
 */
type LinearTriggerStatusUpdated = {
  /**
   * Team ID
   * Use Connect Portal User Settings to allow users to select a Linear Team's ID.  Leaving this blank will trigger this workflow for every public team.
   *
   * **Example**:
   * ```
   * a70bdf0f-530a-4887-857d-46151b52b47c
   * ```
   */
  teamId?: string;

  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Label Added or Removed intent type
 */
type LinearTriggerLabelAdded = {
  /**
   * Team ID
   * Use Connect Portal User Settings to allow users to select a Linear Team's ID.  Leaving this blank will trigger this workflow for every public team.
   *
   * **Example**:
   * ```
   * a70bdf0f-530a-4887-857d-46151b52b47c
   * ```
   */
  teamId?: string;

  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Deleted intent type
 */
type LinearTriggerIssueDeleted = {
  /**
   * Team ID
   * Use Connect Portal User Settings to allow users to select a Linear Team's ID.  Leaving this blank will trigger this workflow for every public team.
   *
   * **Example**:
   * ```
   * a70bdf0f-530a-4887-857d-46151b52b47c
   * ```
   */
  teamId?: string;

  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * New Issue Created intent type
 */
type LinearTriggerIssueCreatedAsApp = {
  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Status Updated intent type
 */
type LinearTriggerStatusUpdatedAsApp = {
  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Label Added or Removed intent type
 */
type LinearTriggerLabelAddedAsApp = {
  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

/**
 * Issue Deleted intent type
 */
type LinearTriggerIssueDeletedAsApp = {
  /**
   * Filter records
   * Only trigger on records that match these filters. Please maintain casing of the string for proper filtering.
   *
   * **Example**:
   * ```
   * Field
   * ```
   */
  filterFormula?: ConditionalInput;

  /**
   * Apply field mapping
   *
   * **Example**:
   * ```
   * {{userSettings.MappingInput.objectMapping}}
   * ```
   */
  objectMapping?: string;
};

export type IntentToInputOutput = {
  /**
   * LINEAR_TRIGGER_ISSUE_CREATED
   */
  LINEAR_TRIGGER_ISSUE_CREATED: [LinearTriggerIssueCreated, any];

  /**
   * LINEAR_TRIGGER_STATUS_UPDATED
   */
  LINEAR_TRIGGER_STATUS_UPDATED: [LinearTriggerStatusUpdated, any];

  /**
   * LINEAR_TRIGGER_LABEL_ADDED
   */
  LINEAR_TRIGGER_LABEL_ADDED: [LinearTriggerLabelAdded, any];

  /**
   * LINEAR_TRIGGER_ISSUE_DELETED
   */
  LINEAR_TRIGGER_ISSUE_DELETED: [LinearTriggerIssueDeleted, any];

  /**
   * LINEAR_TRIGGER_ISSUE_CREATED_AS_APP
   */
  LINEAR_TRIGGER_ISSUE_CREATED_AS_APP: [LinearTriggerIssueCreatedAsApp, any];

  /**
   * LINEAR_TRIGGER_STATUS_UPDATED_AS_APP
   */
  LINEAR_TRIGGER_STATUS_UPDATED_AS_APP: [LinearTriggerStatusUpdatedAsApp, any];

  /**
   * LINEAR_TRIGGER_LABEL_ADDED_AS_APP
   */
  LINEAR_TRIGGER_LABEL_ADDED_AS_APP: [LinearTriggerLabelAddedAsApp, any];

  /**
   * LINEAR_TRIGGER_ISSUE_DELETED_AS_APP
   */
  LINEAR_TRIGGER_ISSUE_DELETED_AS_APP: [LinearTriggerIssueDeletedAsApp, any];
};
