import { DataType, TokenizedSource } from '../../resolvers';
import { ICronStepInitParameters, Schedule } from './cron.interface';
export declare const getCronOptions: (schedule: Schedule, timezone?: string) => ICronStepInitParameters;
export declare const getSchedule: (cron: string, timezone?: string | TokenizedSource<DataType.STRING>, forceCustomSchedule?: boolean) => Schedule;
