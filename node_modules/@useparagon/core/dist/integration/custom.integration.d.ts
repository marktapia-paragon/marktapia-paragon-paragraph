import { SerializedInput } from '../inputs';
import { ICustomIntegration, UserDefinedConfigInput } from './custom.integration.interface';
export declare class CustomIntegration implements ICustomIntegration {
    readonly name: string;
    constructor(name: string);
}
export declare const createConfigInputs: <T extends string = string>(inputs: Record<T, UserDefinedConfigInput>) => Record<T, SerializedInput & {
    toString: () => string;
}>;
type CredentialVariable = {
    toString: () => string;
};
export declare const credentials: {
    oauthAccessToken: CredentialVariable;
    clientId: CredentialVariable;
    clientSecret: CredentialVariable;
    redirectUrl: CredentialVariable;
    scope: CredentialVariable;
    username: CredentialVariable;
    password: CredentialVariable;
    authRequest: {
        params: Record<string, CredentialVariable>;
    };
    tokenRequest: {
        response: Record<string, CredentialVariable>;
    };
    savedCredentials: Record<string, CredentialVariable>;
};
export declare const $uuid: CredentialVariable;
export {};
